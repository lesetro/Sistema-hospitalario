html
  head
    title Recuperar Contraseña
    link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css')
    link(rel='stylesheet' href='/css/style.css')
  body
    include ../layouts/navbar.pug

    .container.mt-4
      if error
        .alert.alert-danger= error
      h1 Recuperar Contraseña
      p Ingresa tu correo electrónico y te enviaremos un enlace para restablecer tu contraseña.
      form(method='POST' action='/auth/recuperar' novalidate)
        .mb-3
          label.form-label(for='email') Correo Electrónico
          input#email.form-control(type='email' name='email' required)
        button.btn.btn-custom(type='submit') Enviar Enlace
      .mt-3
        p ¿Recordaste tu contraseña? 
          a(href='/auth/login' data-link) Inicia sesión aquí
    script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js')
    script(src='/js/script.js')